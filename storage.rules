rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage disabled on Spark plan - no Cloud Storage available
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}