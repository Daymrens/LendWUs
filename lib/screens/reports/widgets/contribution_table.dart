import 'package:flutter/material.dart';

class ContributionTable extends StatelessWidget {
  const ContributionTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
